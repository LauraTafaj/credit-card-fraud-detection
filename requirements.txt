# Core data and machine-learning libraries
pandas>=1.5.0
numpy>=1.21.0
scikit-learn>=1.1.0
imbalanced-learn>=0.9.0     # provides SMOTE for handling class imbalance
matplotlib>=3.5.0
joblib>=1.1.0

# Needed only for the optional web interface (app.py).
streamlit>=1.20.0

# Only needed for the Artificial Neural Network (ANN).
# If you do not want to train the ANN you can skip installing TensorFlow;
# the other three models will still run.
tensorflow>=2.10.0
